// src/html-renderer/render-to-html.ts
import { JsxteRenderError } from "../jsxte-render-error.mjs";
import { jsxElemToHtmlAsync, jsxElemToHtmlSync } from "./jsx-elem-to-html.mjs";
var renderToHtml = (component, options) => {
  try {
    return jsxElemToHtmlSync(component, void 0, options);
  } catch (err) {
    if (JsxteRenderError.is(err)) {
      err.regenerateMessage();
    }
    throw err;
  }
};
var renderToHtmlAsync = async (component, options) => {
  try {
    return await jsxElemToHtmlAsync(await component, void 0, options);
  } catch (err) {
    if (JsxteRenderError.is(err)) {
      err.regenerateMessage();
    }
    throw err;
  }
};
export {
  renderToHtml,
  renderToHtmlAsync
};
